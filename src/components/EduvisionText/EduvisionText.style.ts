import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    readSection: {
        color: 'red'
    },
    blueSection: {
        color: 'blue'
    }

});

export default styles;